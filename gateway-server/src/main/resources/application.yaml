server:
  port: 8000

spring:
  application:
    name: gateway-server
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "http://localhost:8180/realms/master/protocol/openid-connect/certs"
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
          routes:
            - id: USER-SERVICE
              uri:
                "lb://USER-SERVICE"
              predicates:
                - Path= /api/v1/auth/**, /api/v1/users/**, /api/admin/users/**, /users/**

            - id: BOOKING-SERVICE
              uri:
                "lb://BOOKING-SERVICE"
              predicates:
                - Path= /api/v1/bookings/**

            - id: CATEGORY-SERVICE
              uri:
                "lb://CATEGORY-SERVICE"
              predicates:
                - Path= /api/v1/categories/**

            - id: REVIEW-SERVICE
              uri:
                "lb://REVIEW-SERVICE"
              predicates:
                - Path= /api/v1/reviews/**

            - id: PAYMENT-SERVICE
              uri:
                "lb://PAYMENT-SERVICE"
              predicates:
                - Path= /api/v1/payments/**

            - id: SALON-SERVICE
              uri:
                "lb://SALON-SERVICE"
              predicates:
                - Path= /salons/**, /api/v1/salons/**, /api/admin/salons/**

            - id: SERVICE-OFFERING
              uri:
                "lb://SERVICE-OFFERING"
              predicates:
                - Path= /api/v1/service-offerings/**

            - id: NOTIFICATION-SERVICE
              uri:
                "lb://NOTIFICATION-SERVICE"
              predicates:
                - Path= /api/v1/notifications/**

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: "http://localhost:8070/eureka/"